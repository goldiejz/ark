---
phase: 06-cross-customer-learning
plan: 06
type: execute
wave: 5
depends_on:
  - 06-05
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-AOS-31
  - REQ-AOS-32
  - REQ-AOS-33
  - REQ-AOS-34
  - REQ-AOS-35
  - REQ-AOS-36
  - REQ-AOS-37
  - REQ-AOS-38
  - REQ-AOS-39
must_haves:
  truths:
    - ".planning/REQUIREMENTS.md has rows for REQ-AOS-31..REQ-AOS-39 mapping to source files"
    - ".planning/STATE.md Phase 6 section closed with exit-gate evidence"
    - ".planning/ROADMAP.md Phase 6 checkboxes ticked; status flipped to ✅ Complete"
    - "STRUCTURE.md (or vault equivalent) has an AOS Phase 6 contract section"
    - "SKILL.md /ark posture extended with Phase 6 capabilities (best-effort if file present)"
    - "All updates committed to git"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-AOS-31..39 rows"
      contains: "REQ-AOS-31"
    - path: ".planning/STATE.md"
      provides: "Phase 6 close section + retained-tier evidence"
      contains: "Phase 6 — AOS: Cross-Customer Learning Autonomy (complete)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 checkboxes ticked"
      contains: "[x] scripts/lesson-promoter.sh"
  key_links:
    - from: ".planning/REQUIREMENTS.md REQ-AOS-31..39"
      to: "scripts/lesson-promoter.sh + scripts/lib/lesson-similarity.sh + scripts/ark + scripts/ark-deliver.sh + scripts/ark-verify.sh"
      via: "Source-of-truth column"
      pattern: "REQ-AOS-3[1-9]"
---

<objective>
Mint the Phase 6 requirements (REQ-AOS-31..39), close the phase in STATE.md,
flip ROADMAP.md to complete, document the AOS Phase 6 contract in STRUCTURE.md
(or the vault's equivalent location, mirroring how Phases 4 and 5 did it), and
extend the SKILL.md `/ark` posture (best-effort).

Purpose: Phase exit hygiene — covers all 9 acceptance criteria with a paper
trail, mirrors Phase 5's 05-07 closing pattern.

Output: 4–5 doc edits, all committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-cross-customer-learning/CONTEXT.md
@.planning/phases/06-cross-customer-learning/INDEX.md
@.planning/phases/06-cross-customer-learning/06-01-PLAN.md
@.planning/phases/06-cross-customer-learning/06-02-PLAN.md
@.planning/phases/06-cross-customer-learning/06-03-PLAN.md
@.planning/phases/06-cross-customer-learning/06-04-PLAN.md
@.planning/phases/06-cross-customer-learning/06-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update STATE.md, ROADMAP.md, REQUIREMENTS.md, STRUCTURE.md, SKILL.md (best-effort)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, STRUCTURE.md, SKILL.md</files>
  <read_first>
    - .planning/STATE.md (Phase 5 close section — copy structure)
    - .planning/ROADMAP.md (Phase 5 closed-state line — copy format)
    - .planning/REQUIREMENTS.md (existing REQ-AOS-23..30 rows — copy column structure)
    - .planning/phases/05-portfolio-autonomy/05-07-PLAN.md / 05-07-SUMMARY.md if present (mirror)
    - STRUCTURE.md (existing AOS Phase 5 contract section — extend with Phase 6 contract; if file does not exist, check `~/vaults/StrategixMSPDocs/STRUCTURE.md` and similar canonical locations referenced in CLAUDE.md)
    - SKILL.md (existing `/ark` posture — best-effort)
  </read_first>
  <action>
    1. **`.planning/REQUIREMENTS.md`**: append 9 rows. Match the column shape
       used by REQ-AOS-23..30. For each:

       | ID | Statement | Source-of-truth |
       |----|-----------|-----------------|
       | REQ-AOS-31 | scripts/lesson-promoter.sh exists; sourceable; self-test passes | scripts/lesson-promoter.sh |
       | REQ-AOS-32 | scripts/lib/lesson-similarity.sh exposes `lesson_similarity <a> <b>` returning 0..100 | scripts/lib/lesson-similarity.sh |
       | REQ-AOS-33 | Walking $ARK_PORTFOLIO_ROOT/*/tasks/lessons.md produces a candidate set | scripts/lesson-promoter.sh::promoter_scan_lessons |
       | REQ-AOS-34 | Patterns ≥2 customers + ≥60% similarity → auto-promoted to ~/vaults/ark/lessons/universal-patterns.md | scripts/lesson-promoter.sh::promoter_apply_pending |
       | REQ-AOS-35 | Anti-patterns auto-promoted to ~/vaults/ark/bootstrap/anti-patterns.md | scripts/lesson-promoter.sh::promoter_apply_pending |
       | REQ-AOS-36 | Every promotion audit-logged via `_policy_log "lesson_promote" "PROMOTED" ...` | scripts/lesson-promoter.sh + scripts/ark-policy.sh |
       | REQ-AOS-37 | `ark promote-lessons` subcommand exists (manual run, --full or --since) | scripts/ark + scripts/ark-deliver.sh |
       | REQ-AOS-38 | Tier 12 verify: synthetic 3-customer fixture asserts correct promotion | scripts/ark-verify.sh tier 12 |
       | REQ-AOS-39 | Existing Tier 1–11 still pass (no regression) | scripts/ark-verify.sh |

    2. **`.planning/STATE.md`**: 
       - Bump frontmatter: `completed_phases: 6`, `total_plans: 47` (if 06 has 6 plans), `percent: 86`, `last_updated: <ISO-now>`, `current_phase: "Phase 6 (AOS: Cross-Customer Learning Autonomy)"`, `status: complete`.
       - Append a new "## Phase 6 — AOS: Cross-Customer Learning Autonomy (complete)" section mirroring Phase 5's. Include:
         - Goal sentence
         - Exit gate sentence with concrete tier results from 06-05's regression sweep
         - Plan-by-plan outcome table (06-01..06-06)
       - Update the "Phase 6+ — Future" section: replace it with "Phase 7 — Continuous Operation" pointer.

    3. **`.planning/ROADMAP.md`**: 
       - Tick all checkboxes under "### Phase 6 — AOS: Cross-Customer Learning Autonomy".
       - Append a "**Status:** ✅ Complete — see .planning/phases/06-cross-customer-learning/" line under the exit-criteria sentence (matching Phase 5's pattern).

    4. **STRUCTURE.md (or vault equivalent)**: append a new section "## AOS Phase 6 — Cross-Customer Learning Contract" describing:
       - Discovery scope: `$ARK_PORTFOLIO_ROOT/*/tasks/lessons.md`
       - Similarity primitive: Jaccard token overlap, 0..100 integer
       - Promotion thresholds: ≥2 customers, ≥3 occurrences, ≥60% similarity
       - Routing: `lessons/universal-patterns.md` vs `bootstrap/anti-patterns.md` (anti-pattern detection by title-string)
       - Audit class: `lesson_promote`; decisions: `PROMOTED | DEPRECATED | MEDIOCRE_KEPT_PER_CUSTOMER`
       - Idempotency invariant + atomic-write + mkdir-lock discipline
       - Out-of-scope explicit: ML embeddings, customer-specific redaction, cross-customer deprecation of per-customer lessons

       If the canonical STRUCTURE.md is in a vault path (per CLAUDE.md
       guidance, `~/vaults/StrategixMSPDocs/`), update the local repo
       STRUCTURE.md only if it exists; otherwise note in the SUMMARY that the
       contract belongs in the vault doc and was added there. Do NOT create a
       new file at the repo root if STRUCTURE.md doesn't already exist there.

    5. **SKILL.md** (best-effort): if `SKILL.md` exists at repo root or
       `~/vaults/ark/SKILL.md`, append a "Phase 6 posture" stanza:
       - "/ark now consumes cross-customer patterns via universal-patterns.md and bootstrap/anti-patterns.md"
       - "ark promote-lessons available for manual runs"
       - "Post-phase auto-promotion runs after every successful ark deliver phase"

       If neither exists, log "SKILL.md not present — skipped" in the SUMMARY
       and continue (this is best-effort per Plan 05-07 precedent).

    6. **Commit**:
       ```
       git add .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md \
               STRUCTURE.md SKILL.md 2>/dev/null
       git commit -m "AOS Phase 6: close out — REQ-AOS-31..39 minted; STATE/ROADMAP/STRUCTURE updated"
       ```
       (`SKILL.md` and `STRUCTURE.md` may legitimately not exist; `git add`
       will silently no-op for missing paths, and the commit will succeed
       with whichever files DO exist.)

    Avoid:
    - Editing source code in this plan (docs-only).
    - Reverting any earlier plan's tick-box state.
    - Creating new files at unconventional paths (STRUCTURE.md, SKILL.md
      should already exist; if not, note in SUMMARY and skip).
    - Bumping STATE.md `total_plans` without verifying actual plan count.
  </action>
  <verify>
    <automated>grep -c 'REQ-AOS-3[1-9]' .planning/REQUIREMENTS.md | awk '$1==9 { exit 0 } { exit 1 }' && grep -q 'Phase 6 — AOS: Cross-Customer Learning Autonomy (complete)' .planning/STATE.md && grep -q 'Phase 6.*Complete' .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - `.planning/REQUIREMENTS.md` contains 9 new rows (REQ-AOS-31..39), each with a Source-of-truth pointer
    - `.planning/STATE.md` shows Phase 6 closed with plan-table and tier-retention numbers
    - `.planning/ROADMAP.md` Phase 6 checkboxes ticked, status flipped to ✅ Complete
    - STRUCTURE.md (or vault equivalent) has AOS Phase 6 contract section (or SUMMARY explains why deferred to vault)
    - SKILL.md updated if present (best-effort; documented in SUMMARY otherwise)
    - All five files committed in a single commit; commit message references "AOS Phase 6: close out"
  </done>
</task>

</tasks>

<verification>
```bash
# Requirement coverage
grep -c 'REQ-AOS-3[1-9]' .planning/REQUIREMENTS.md   # → 9

# STATE close
grep -q 'Phase 6 — AOS: Cross-Customer Learning Autonomy (complete)' .planning/STATE.md

# ROADMAP flip
grep -q 'Phase 6.*Complete\|Phase 6 — AOS: Cross-Customer Learning Autonomy.*\[x\]' .planning/ROADMAP.md \
  || grep -A2 '### Phase 6' .planning/ROADMAP.md | grep -q '\[x\]'

# Commit landed
git log --oneline -1 | grep -qE 'Phase 6.*close|close out'
```
</verification>

<success_criteria>
- 9/9 REQ-AOS-31..39 rows present in REQUIREMENTS.md
- STATE.md frontmatter advances (completed_phases 6, percent ≥ 85)
- ROADMAP.md Phase 6 status flipped
- STRUCTURE.md AOS Phase 6 contract added (or vault equivalent — documented)
- SKILL.md updated if present (best-effort)
- Single git commit captures all doc updates
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-customer-learning/06-06-SUMMARY.md`
documenting:
- Final REQUIREMENTS row count for REQ-AOS-31..39
- STATE.md frontmatter delta (before/after)
- ROADMAP.md checkbox states (all 6 ticked)
- Where the STRUCTURE.md AOS Phase 6 contract landed (repo or vault)
- Whether SKILL.md was updated
- Commit hash + message
- Final phase exit declaration: "Phase 6 closed — REQ-AOS-31..39 satisfied; Tier 12 16/16; Tiers 1–11 retained."
</output>
