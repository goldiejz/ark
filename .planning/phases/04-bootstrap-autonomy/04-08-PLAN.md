---
phase: 04-bootstrap-autonomy
plan: 08
type: execute
wave: 6
depends_on:
  - "04-07"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-AOS-15
  - REQ-AOS-16
  - REQ-AOS-17
  - REQ-AOS-18
  - REQ-AOS-19
  - REQ-AOS-20
  - REQ-AOS-21
  - REQ-AOS-22
must_haves:
  truths:
    - "REQUIREMENTS.md has rows REQ-AOS-15 through REQ-AOS-22 with status=done and evidence pointing to commits/files in this phase"
    - "STATE.md shows Phase 4 complete with the 8-plan outcomes table mirroring Phase 2/3 structure"
    - "ROADMAP.md Phase 4 checkboxes are marked complete"
    - "If STRUCTURE.md and SKILL.md exist (vault-level docs), they receive an AOS Bootstrap Contract section. If they do not exist, this plan creates them best-effort or notes the absence in the SUMMARY."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "8 new rows REQ-AOS-15..REQ-AOS-22"
    - path: ".planning/STATE.md"
      provides: "Phase 4 marked complete; outcomes table; current_phase advanced to Phase 5 (next per ROADMAP)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 checkboxes marked complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "code commits + scripts/bootstrap-policy.sh + scripts/ark-create.sh + Tier 10"
      via: "evidence column referencing file paths and tier results"
      pattern: "REQ-AOS-(15|16|17|18|19|20|21|22)"
---

<objective>
Document Phase 4. Mint REQ-AOS-15 through REQ-AOS-22 in REQUIREMENTS.md. Mark Phase 4 complete in STATE.md and ROADMAP.md. Best-effort: extend STRUCTURE.md and SKILL.md with the AOS Bootstrap Autonomy contract section if those files exist at the vault level.

Purpose: Closes Phase 4. Establishes the audit trail Phase 5+ will reference when the portfolio engine needs to understand "what does bootstrap autonomy look like".
Output: Updated planning docs; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bootstrap-autonomy/CONTEXT.md
@.planning/phases/04-bootstrap-autonomy/INDEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append REQ-AOS-15..REQ-AOS-22 to .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing rows — match the column shape of REQ-AOS-08..REQ-AOS-14)
  </read_first>
  <action>
    Append exactly 8 rows to the end of the existing requirements table. Match the existing column shape: `| ID | Requirement | Status | Evidence |`.

    ```markdown
    | REQ-AOS-15 | scripts/bootstrap-policy.sh exists; sourceable; self-test passes | done | scripts/bootstrap-policy.sh; `bash scripts/bootstrap-policy.sh test` exits 0 |
    | REQ-AOS-16 | `ark create "<one-line description>" --customer <name>` runs to completion with zero prompts | done | scripts/ark-create.sh description-mode block; Tier 10 fixtures 1-5 |
    | REQ-AOS-17 | Inferred type/stack/deploy logged via `_policy_log "bootstrap" ...` with full context | done | scripts/bootstrap-policy.sh::bootstrap_classify; scripts/ark-create.sh RESOLVED_FINAL/FLAG_OVERRIDE emissions |
    | REQ-AOS-18 | Per-project `.planning/policy.yml` auto-generated with inferred values | done | scripts/ark-create.sh policy.yml.tmp.$$ + mv pattern; Tier 10 grep-asserts |
    | REQ-AOS-19 | CLAUDE.md atomically written from base + project-type addendum + customer footer | done | bootstrap/claude-md-template.md + bootstrap/claude-md-addendum/*; scripts/ark-create.sh sed-pipeline + mv |
    | REQ-AOS-20 | Existing `ark create` flag-based invocation still works (backward compat) | done | scripts/ark-create.sh flag-mode preserved; Tier 1-9 regression pass |
    | REQ-AOS-21 | Tier 10 verify: 5 different project types, no prompts, all produce valid scaffolds | done | scripts/ark-verify.sh Tier 10 section; 5 fixtures × ~6 sub-checks each |
    | REQ-AOS-22 | Existing Tier 1–9 still pass (no regression) | done | Tier 7 (14/14), Tier 8 (25/25), Tier 9 (20/20) post-Phase-4 |
    ```

    Confirm row count went from 14 (current REQ-AOS rows) to 22:
    `grep -c '^| REQ-AOS' .planning/REQUIREMENTS.md` should return 22.

    Do not modify or remove existing rows.
  </action>
  <verify>
    <automated>grep -c '^| REQ-AOS' /Users/jongoldberg/vaults/automation-brain/.planning/REQUIREMENTS.md | grep -qx 22</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md has exactly 22 `| REQ-AOS-` rows (was 14, added 8).
    - Each new row has all 4 pipe-separated columns.
    - Status column for all 8 new rows is `done`.
  </acceptance_criteria>
  <done>
    All 8 Phase 4 requirement IDs minted with traceable evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/STATE.md — Phase 4 close + outcomes table</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (existing structure — find the "Phase 3" section; mirror its outcomes-table shape for Phase 4)
  </read_first>
  <action>
    1. Update YAML frontmatter:
       ```yaml
       current_phase: "Phase 4 (AOS: Bootstrap Autonomy)"
       status: complete
       last_updated: "<current ISO8601 timestamp>"
       progress:
         total_phases: 7  # Phases 1-7 are AOS journey + Phase 8 hardening; treat AOS as 7-phase per ROADMAP
         completed_phases: 4
         total_plans: 34   # 26 + 8 from Phase 4
         completed_plans: 33  # +8 from Phase 4
         percent: 97
       ```
       Adjust the totals to whatever is accurate; do not invent numbers — count from the phases dir.

    2. Add a new section after "Phase 3 — AOS: Self-Improving Self-Heal":
       ```markdown
       ## Phase 4 — AOS: Bootstrap Autonomy (complete)

       See `.planning/phases/04-bootstrap-autonomy/`

       **Goal:** `ark create "<one-line description>" --customer <name>` runs to completion with zero prompts. Inference + audit + atomic CLAUDE.md/policy.yml writes; backward-compat for flag-mode.

       **Exit gate:** Tier 10 5-fixture suite passes + Tier 1–9 still pass (`scripts/ark-verify.sh`).

       | Plan | Outcome |
       |------|---------|
       | 04-01 | `scripts/bootstrap-policy.sh`: keyword-overlap inference engine; `bootstrap_classify` orchestrator emitting TSV verdict + `_policy_log "bootstrap"` audit; confidence threshold escalates `architectural-ambiguity` to ESCALATIONS.md; bash 3 compat |
       | 04-02 | `bootstrap/project-types/*-template.md` frontmatter tagged with `keywords:`, `default_stack:`, `default_deploy:`; new `custom-template.md` catch-all |
       | 04-03 | `bootstrap/claude-md-template.md` base + 4 `claude-md-addendum/<type>.md` files (service-desk, revops, ops-intelligence, custom); anchor-based composition |
       | 04-04 | `scripts/ark-create.sh` description-mode wired; sed-pipeline + atomic mv for CLAUDE.md; auto-generated `.planning/policy.yml`; `_policy_log "bootstrap" RESOLVED_FINAL/FLAG_OVERRIDE` audit |
       | 04-05 | `scripts/lib/bootstrap-customer.sh` mkdir-lock customer dir + idempotent seed; `scripts/lib/policy-config.sh` extended with customer layer (env > project > customer > vault > default) |
       | 04-06 | `scripts/ark` dispatcher `cmd_create` pass-through; help text reflects description-mode |
       | 04-07 | Tier 10 verify suite (5 fixtures × ~6 sub-checks; isolated tmp vault; real DB md5 unchanged); Tier 1-9 regression sweep clean |
       | 04-08 | REQ-AOS-15..REQ-AOS-22 minted; STATE.md Phase 4 close; ROADMAP.md checkboxes; STRUCTURE.md + SKILL.md best-effort |
       ```

    3. Update the trailing "Phase 4+ — Future" section to advance the next-phase pointer to Phase 5 (Portfolio Autonomy).
  </action>
  <verify>
    <automated>grep -q "Phase 4 — AOS: Bootstrap Autonomy (complete)" /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md && grep -q "current_phase: \"Phase 4" /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md && grep -q "04-04" /Users/jongoldberg/vaults/automation-brain/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter has `current_phase: "Phase 4 (AOS: Bootstrap Autonomy)"` and `status: complete`.
    - New "Phase 4" section present with 8-row outcomes table.
    - Phase 1, 2, 3 sections unchanged.
    - "Phase 4+ Future" section advanced to point to Phase 5.
  </acceptance_criteria>
  <done>
    STATE.md is the new primary truth that Phase 4 is shipped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 4 checkboxes complete in .planning/ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the "Phase 4 — AOS: Bootstrap Autonomy" section with `- [ ]` checkboxes)
  </read_first>
  <action>
    Mark each checkbox in the "Phase 4" section as `- [x]`:
    - bootstrap-policy.sh inference engine
    - Heuristics from brain templates become policy rules
    - Per-project-type defaults
    - Auto-resolves contradictions (best-effort — uses customer policy + fallback to default)
    - Strip prompts from ark-create.sh
    - Per-project policy.yml auto-generated
    - Tier 10 verify

    Do NOT modify Phase 1, 2, 3 sections or any other phase's checkboxes.
  </action>
  <verify>
    <automated>awk '/^### Phase 4/,/^### Phase 5/' /Users/jongoldberg/vaults/automation-brain/.planning/ROADMAP.md | grep -c '^- \[x\]' | awk '$1>=6{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Phase 4 section has ≥ 6 `- [x]` checkboxes.
    - Phase 4 section has 0 `- [ ]` checkboxes (all complete).
    - Other phases' checkboxes unchanged.
  </acceptance_criteria>
  <done>
    ROADMAP.md reflects Phase 4 complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Best-effort STRUCTURE.md + SKILL.md updates (skip if absent)</name>
  <files>(STRUCTURE.md, SKILL.md — vault-level locations TBD by executor)</files>
  <read_first>
    - .planning/phases/03-self-improving-self-heal/INDEX.md (mentions STRUCTURE.md and SKILL.md updates in plan 03-08 — confirm whether those files were actually created in Phase 3)
  </read_first>
  <action>
    Search for vault-level STRUCTURE.md and SKILL.md:
    ```bash
    find /Users/jongoldberg/vaults/automation-brain -maxdepth 3 -iname "STRUCTURE.md" -o -iname "SKILL.md" 2>/dev/null
    ```

    **If both files exist:** Append a section titled `## AOS Bootstrap Autonomy Contract (Phase 4)` to each, summarizing:
    - `bootstrap_classify` API surface from 04-01
    - `_policy_log "bootstrap"` class semantics (CLASSIFY_CONFIDENT, RESOLVED_FINAL, FLAG_OVERRIDE)
    - Cascading customer policy.yml layer position (env > project > customer > vault > default)
    - Atomic-write discipline for CLAUDE.md / policy.yml / package.json
    - Tier 10 acceptance shape

    **If files do not exist:** Document in the Phase 4 SUMMARY (next task) that vault-level STRUCTURE.md and SKILL.md are absent and that this is acceptable — Phase 3's 03-08 plan referenced them but they may have been created elsewhere or the plan deferred them. Do NOT create them speculatively in this task; that's a separate documentation concern.

    Either path: this task is best-effort and never fails the plan.
  </action>
  <verify>
    <automated>echo "best-effort task; passes regardless of file existence — see SUMMARY for outcome"</automated>
  </verify>
  <acceptance_criteria>
    - If STRUCTURE.md exists, it contains an `## AOS Bootstrap Autonomy Contract (Phase 4)` section after this task.
    - If SKILL.md exists, same.
    - If neither exists, the Phase 4 SUMMARY documents the absence (no fabrication of files).
    - This task does not block plan completion either way.
  </acceptance_criteria>
  <done>
    Best-effort doc parity with Phase 3.
  </done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md has 22 REQ-AOS rows.
- STATE.md frontmatter shows Phase 4 complete.
- ROADMAP.md Phase 4 section all checked.
- Tier 10 still passes (no regression from doc-only changes).
</verification>

<success_criteria>
- All 8 acceptance criteria from CONTEXT.md have a REQ-AOS-* row with status=done.
- STATE.md is the live truth that Phase 4 is shipped; matches the verification artifact (Tier 10 passing).
- Phase 5 (Portfolio Autonomy) is now the next-up phase.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bootstrap-autonomy/04-bootstrap-autonomy-08-SUMMARY.md` with:
- Final REQ-AOS row table (verbatim copy)
- Tier results from 04-07 (Tier 7/8/9/10)
- STRUCTURE.md / SKILL.md outcome (updated, created, or absent — be honest)
- Pointer to Phase 5 next steps per ROADMAP.md
</output>
