---
phase: 04-bootstrap-autonomy
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - bootstrap/project-types/service-desk-template.md
  - bootstrap/project-types/revops-template.md
  - bootstrap/project-types/ops-intelligence-template.md
  - bootstrap/project-types/custom-template.md
autonomous: true
requirements:
  - REQ-AOS-17
must_haves:
  truths:
    - "Each project-type template has YAML frontmatter declaring `keywords:`, `default_stack:`, `default_deploy:`, and `project_type:`"
    - "Keyword set per template is non-trivial (≥ 8 distinct keywords) and reflects how a user would describe that project type in plain English"
    - "Custom template exists as the catch-all with empty keywords (intentional — never wins on keyword overlap)"
  artifacts:
    - path: "bootstrap/project-types/service-desk-template.md"
      provides: "Updated frontmatter: keywords, default_stack=vite-react-hono, default_deploy=cloudflare-workers"
    - path: "bootstrap/project-types/revops-template.md"
      provides: "Updated frontmatter: keywords, default_stack=vite-react-hono, default_deploy=cloudflare-workers"
    - path: "bootstrap/project-types/ops-intelligence-template.md"
      provides: "Updated frontmatter: keywords, default_stack=nextjs-on-workers, default_deploy=cloudflare-workers"
    - path: "bootstrap/project-types/custom-template.md"
      provides: "NEW catch-all template; empty keywords; default_stack=custom, default_deploy=none"
  key_links:
    - from: "scripts/bootstrap-policy.sh::bootstrap_infer_type"
      to: "bootstrap/project-types/*.md frontmatter"
      via: "awk-frontmatter scan, keyword tokenization"
      pattern: "keywords:"
---

<objective>
Tag the existing project-type templates with machine-readable frontmatter that the inference engine (04-01) consumes. Without this data, `bootstrap_infer_type` always returns 0% confidence and everything escalates.

Purpose: Give the keyword-overlap classifier real signal to score against.
Output: 3 updated templates + 1 new catch-all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bootstrap-autonomy/CONTEXT.md
@.planning/phases/04-bootstrap-autonomy/INDEX.md
@.planning/phases/04-bootstrap-autonomy/04-01-PLAN.md
@bootstrap/project-types/service-desk-template.md
@bootstrap/project-types/revops-template.md
@bootstrap/project-types/ops-intelligence-template.md
@scripts/bootstrap-policy.sh

<interfaces>
04-01's `_bp_read_template_keywords` extracts the comma-separated `keywords:` value from frontmatter. `_bp_read_template_field` extracts arbitrary scalars. Both expect:

```yaml
---
project_type: <slug>
default_stack: <stack-name>
default_deploy: <deploy-target>
keywords: comma, separated, list, of, lowercase, terms
# any other fields preserved
---
```

Frontmatter MUST be the first block of the file (between `---` markers, starting at line 1). Existing templates already follow this — only the field list is being expanded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag the 3 existing templates with keywords + default_stack + default_deploy</name>
  <files>
    bootstrap/project-types/service-desk-template.md,
    bootstrap/project-types/revops-template.md,
    bootstrap/project-types/ops-intelligence-template.md
  </files>
  <read_first>
    - bootstrap/project-types/service-desk-template.md (entire file — understand existing frontmatter shape and content)
    - bootstrap/project-types/revops-template.md (full)
    - bootstrap/project-types/ops-intelligence-template.md (full)
  </read_first>
  <action>
    For each of the 3 existing templates, edit ONLY the YAML frontmatter block (between the leading `---` markers). Preserve the existing `project_type`, `source_project`, `template_version`, `created` fields exactly. Append three new fields if missing, or update if present:

    - `default_stack:` (string, no quotes needed)
    - `default_deploy:` (string)
    - `keywords:` (comma-separated, lowercase, no quotes)

    DO NOT modify any other content in any template. The body is the human-readable bootstrap guidance — leave it.

    **service-desk-template.md** (project_type: service-desk):
    ```yaml
    default_stack: vite-react-hono
    default_deploy: cloudflare-workers
    keywords: service desk, helpdesk, help desk, ticket, tickets, ticketing, sla, slas, itil, incident, incidents, problem management, change management, msp, managed service, customer support, technician, engineer workload, timesheet, sign-off, helpdesk platform
    ```

    **revops-template.md** (project_type: revops):
    ```yaml
    default_stack: vite-react-hono
    default_deploy: cloudflare-workers
    keywords: revops, rev ops, revenue operations, crm, quotes, quoting, pipeline, opportunity, opportunities, deal, deals, commission, commissions, billing, invoicing, margin, approval, approvals, sales pipeline, account management, contract, contracts, renewal, renewals
    ```

    **ops-intelligence-template.md** (project_type: ops-intelligence):
    ```yaml
    default_stack: nextjs-on-workers
    default_deploy: cloudflare-workers
    keywords: ops intelligence, ops dashboard, msp dashboard, monitoring, monitoring dashboard, observability, n-central, halopsa, kpi, kpis, sla dashboard, technician workload, incident dashboard, client health, operations dashboard, executive dashboard, status board, network monitoring
    ```

    Atomicity: edit each file in-place via the Edit/Write tool — the executor should use the Edit tool to replace just the frontmatter block (target: the lines between the first two `---` markers). Do not rewrite the whole file.

    **Verification command** (executor runs after each edit):
    `awk '/^---$/{c++} c==1{print} c==2{exit}' bootstrap/project-types/<file>.md | grep -E '^(default_stack|default_deploy|keywords|project_type):'` should print exactly 4 lines.
  </action>
  <verify>
    <automated>for f in service-desk revops ops-intelligence; do awk '/^---$/{c++} c==1{print} c==2{exit}' /Users/jongoldberg/vaults/automation-brain/bootstrap/project-types/${f}-template.md | grep -cE '^(default_stack|default_deploy|keywords|project_type):' | grep -qx 4 || { echo "FAIL: $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 templates has `default_stack:`, `default_deploy:`, `keywords:`, `project_type:` lines in the frontmatter.
    - `keywords:` line for each contains ≥ 15 comma-separated tokens.
    - All other frontmatter fields preserved (`source_project`, `template_version`, `created`).
    - File body (everything after the second `---`) is byte-identical to before the edit (use `git diff --stat` — only frontmatter section should change).
    - Sourcing 04-01's bootstrap-policy.sh and running `bootstrap_infer_type "service desk for acme with sla itil"` against the real vault returns `service-desk\t<score>` with score ≥ 50.
  </acceptance_criteria>
  <done>
    All 3 templates are machine-readable. The classifier has signal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create bootstrap/project-types/custom-template.md — catch-all</name>
  <files>bootstrap/project-types/custom-template.md</files>
  <read_first>
    - bootstrap/project-types/service-desk-template.md (frontmatter shape + body section structure to mirror minimally)
  </read_first>
  <action>
    Create `bootstrap/project-types/custom-template.md`. Frontmatter:

    ```yaml
    ---
    project_type: custom
    source_project: none
    template_version: 1.0
    created: 2026-04-26
    default_stack: custom
    default_deploy: none
    keywords:
    ---
    ```

    Note: `keywords:` is intentionally EMPTY. This template MUST never win on keyword overlap; it exists so `bootstrap_infer_stack custom` and `bootstrap_infer_deploy custom` resolve cleanly.

    Body:
    ```markdown
    # Custom / Scratch Project Bootstrap Template

    Catch-all for projects that don't match a known type.

    Used when:
    - User invokes `ark create "scratchpad for X"` with no recognizable project-type keywords.
    - Inference confidence is below threshold and user accepts the escalation by overriding to `--type custom`.

    Stack: minimal — `package.json` only (or `go.mod`, etc., depending on user override).
    Deploy: none. User must explicitly set `--deploy` if they want one.

    No project-section / purpose-section / scope-section guidance — this template intentionally provides no opinions, because the project's purpose is unknown.
    ```

    File is plain markdown. No execute bit.
  </action>
  <verify>
    <automated>test -f /Users/jongoldberg/vaults/automation-brain/bootstrap/project-types/custom-template.md && awk '/^---$/{c++} c==1{print} c==2{exit}' /Users/jongoldberg/vaults/automation-brain/bootstrap/project-types/custom-template.md | grep -cE '^(default_stack|default_deploy|keywords|project_type):' | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - Frontmatter has `project_type: custom`, `default_stack: custom`, `default_deploy: none`, `keywords:` (empty value).
    - Body is non-empty (≥ 5 lines after frontmatter).
    - Running `bootstrap_infer_type "<any random non-matching string>"` against the real vault now returns either `custom\t0` or another type with score 0 (never blows up because `*-template.md` set is non-empty).
  </acceptance_criteria>
  <done>
    Catch-all template exists; classifier never returns an undefined type.
  </done>
</task>

</tasks>

<verification>
- `bash -c 'source scripts/bootstrap-policy.sh; bootstrap_infer_type "service desk for acme with itil"'` against real vault → first TSV field `service-desk`, score ≥ 50.
- `bash -c 'source scripts/bootstrap-policy.sh; bootstrap_infer_type "revops crm pipeline for acme"'` → first field `revops`, score ≥ 30.
- `bash -c 'source scripts/bootstrap-policy.sh; bootstrap_infer_type "ops intelligence dashboard with halopsa"'` → first field `ops-intelligence`, score ≥ 30.
- `git diff bootstrap/project-types/*.md` shows ONLY frontmatter changes for the 3 existing files (no body churn).
</verification>

<success_criteria>
- 04-01's classifier now produces real verdicts against the real vault, not just against the synthetic self-test fixtures.
- Custom template exists as a stable fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bootstrap-autonomy/04-bootstrap-autonomy-02-SUMMARY.md` listing the keyword sets chosen + any classifier-score sanity checks against the real vault.
</output>
